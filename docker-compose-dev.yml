services:
  postgres:
    image: postgres:16-alpine
    container_name: guia-turismo-postgres-dev
    restart: unless-stopped
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: guia_turismo_dev
    ports:
      - '5432:5432'
    volumes:
      - postgres_data_dev:/var/lib/postgresql/data
    healthcheck:
      test: ['CMD-SHELL', 'pg_isready -U postgres -d guia_turismo_dev']
      interval: 10s
      timeout: 5s
      retries: 5

volumes:
  postgres_data_dev:
